// Chamada de função

// const minhaSaudacao = (nome, cidade, idade) => {

// 	return `Olá de ${nome}, que possui ${idade} anos de idade e reside em ${cidade}`
// };

// const vivente = ["João", "Boston", 40];

// console.log(minhaSaudacao(...vivente));

// Exemplo 2: concatenar arrays

const a = [1, 2, 3];
const b = [4, 5];

const c = a.concat(b);

console.log(c);

const d = [...a, ...b];

// Copiar array

